name: Deploy slave

on:
  push:
    branches: [main]
    paths:
      - 'slave/**'
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy slave to server
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.DEPLOY_HOST }}
          username: ${{ secrets.DEPLOY_USER }}
          key: ${{ secrets.DEPLOY_KEY }}
          script: |
            cd ~/doohub
            git pull origin main
            docker compose -f docker-compose.prod.yml build slave
            docker compose -f docker-compose.prod.yml up -d slave
